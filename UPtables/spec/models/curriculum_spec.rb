require 'rails_helper'

RSpec.describe Curriculum, type: :model do
  
  #
  #let(:curr) { create :curriculum_with_curriculum_module_assignments }
  #
  #
  #
  #let(:cur) { create :curriculum}
  #
  #let(:mod1) { create :ects_module }
  #let(:mod2) { create :ects_module }
  #let(:mod3) { create :ects_module }
  #
  #let(:cma1) { create :curriculum_module_assignment, :curriculum => cur, :ects_module => mod1, :mandatory => true  } 
  #let(:cma2) { create :curriculum_module_assignment, :curriculum => cur, :ects_module => mod2, :mandatory => false } 
  #let(:cma3) { create :curriculum_module_assignment, :curriculum => cur, :ects_module => mod3, :mandatory => false } 
  #
  #describe "#mandatory_modules" do
  #  it "lists all mandatory modules" do 
  #    expect(cur.mandatory_modules).to match_array([mod1])
  #  end
  #end
  #
  #describe "#selectable_modules" do
  #  it "lists all selectable modules" do 
  #    expect(cur.selectable_modules).to match_array([mod1])
  #  end
  #end
  #

end
